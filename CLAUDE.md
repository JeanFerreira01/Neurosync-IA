# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Environment

- Python 3.14.4 (located at `C:\Python314\python.exe`)
- Virtual environment is at `venv\` — activate with `.\venv\Scripts\Activate.ps1` (PowerShell) before running any Python commands
- Project name: **projetoclinica** (clinic management system)

## Project Status

This project is in its initial setup phase. Only the virtual environment exists so far — no source files, dependencies, or tests have been added yet.
